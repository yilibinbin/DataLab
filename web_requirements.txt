# Web app install. The single source of truth is pyproject.toml — this file is a
# thin pointer to the matching extras so the two can't drift (P2-2).
# `.[web,collab,mcmc]` = core deps + Flask/mistune/waitress + flask-socketio
# (real-time collaboration) + MCMC (numpy/emcee/corner), so the web
# "Refine with MCMC" toggle works. emcee stays behind a HAS_EMCEE runtime guard,
# so the stack still imports cleanly if these are absent.
-e .[web,collab,mcmc]
