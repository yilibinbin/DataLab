# Desktop GUI install. The single source of truth is pyproject.toml — this file
# is a thin pointer to the matching extras so the two can't drift (P2-2).
# `.[desktop,mcmc]` = core deps (mpmath/sympy/scipy/Pillow/certifi/pyyaml/
# cryptography) + PySide6 + MCMC (numpy/emcee/corner), so the GUI's
# "Refine with MCMC" checkbox works out of the box.
-e .[desktop,mcmc]
