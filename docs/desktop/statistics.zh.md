# 统计平均

统计平均模块用于对一列数据进行统计与均值估计，并可在存在不确定度信息时进行加权统计。

## 输入与模式

- 选择需要统计的数值列
- 如存在不确定度信息，可进行加权平均等统计（默认权重为 `w=1/σ²`）
- 对缺失或不合法的不确定度数据行会自动忽略，并在日志给出提示
  - 若存在 `σ=0`：视为“无限权重锚点”；若多个 `σ=0` 数值不一致会报错

## 输出内容

结果区会根据所选模式给出：

- 平均值与对应的不确定度估计
- 标准差、最小值、最大值等统计量
- 加权有效样本数 `n_eff`（Kish 公式）等附加信息（如适用）

## 导出与排版

- 可导出结果表格
- 可生成用于排版的结果文本与文档预览
