# Docs install. The single source of truth is pyproject.toml — this file is a
# thin pointer to the matching extra so the two can't drift (P2-2).
# `.[docs]` = mkdocs / mkdocs-material / pymdown-extensions.
-e .[docs]
